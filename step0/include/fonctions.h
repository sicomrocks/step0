#include <string.h>
#include "memoire.h"
#include "notify.h"

/*fonction pour regarder si le paramètre est bien un nom de registre.
 * Si oui, elle renvoie 1, si non elle renvoie 0*/
int isregister(char* param);
